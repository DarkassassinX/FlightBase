#include<iostream>
using namespace std;
int main(int argc, char){
drone.take_off(10.0)
    drone.position_set(10, 5, 10, relative=True);
    drone.position_set(0, 10, 10, relative=True);
    drone.position_set(0, 0, 10, relative=True);
    drone.land(0);
}